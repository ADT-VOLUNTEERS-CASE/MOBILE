package org.adt.presentation.navigation

import androidx.navigation.NavHostController

// Ignore in release(overridden in debug source set)
@Suppress("unused")
context(navController: NavHostController)
fun debugGraph() {
    // Empty
}